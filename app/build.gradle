apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'greenDic'
            keyPassword 'q1w2e3r4'
            storeFile file('/Users/preparkha/Documents/dev/android_keystore/green_thai_kor_dic/green_keystore.jks')
            storePassword 'q1w2e3r4'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "com.ph.greenkorthaidictionary"
        minSdkVersion 14
        targetSdkVersion 22
        versionCode 21
        versionName "1.1.3"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.2.0'
    compile files('libs/gson-2.3.1.jar')
    compile files('libs/volley.jar')
    compile('org.apache.httpcomponents:httpmime:4.3') {
        exclude module: "httpclient"
    }
    compile files('libs/tts_reference-dex2jar.jar')
}
